# Frappe All-in-One Configuration
# Kopiere diese Datei nach .env und passe die Werte an

# ===========================================
# WICHTIG: Diese Werte MÜSSEN angepasst werden
# ===========================================

# Deine Domain (ohne https://)
SITE_NAME=erp.example.com

# Admin-Passwort für Frappe/ERPNext
ADMIN_PASSWORD=SuperSecurePassword123!

# MySQL Root-Passwort
MYSQL_ROOT_PASSWORD=AnotherSecurePassword456!

# ===========================================
# Image-Konfiguration
# ===========================================

# Docker Image (nach dem ersten Build anpassen!)
# Format: ghcr.io/DEIN-GITHUB-USERNAME/frappe-allinone
IMAGE_NAME=ghcr.io/danmitrea/frappe_allinone

# Image Version
VERSION=latest

# Pull Policy (always, never, if-not-present)
PULL_POLICY=always

# ===========================================
# Apps die installiert werden sollen
# ===========================================

# Alle Apps (Standard):
INSTALL_APP_ARGS=--install-app erpnext --install-app hrms --install-app crm --install-app helpdesk --install-app insights --install-app drive --install-app builder --install-app lms --install-app wiki --install-app gameplan --install-app print_designer

# Nur ERPNext + HR:
# INSTALL_APP_ARGS=--install-app erpnext --install-app hrms

# Nur ERPNext:
# INSTALL_APP_ARGS=--install-app erpnext

# ===========================================
# Optionale Einstellungen
# ===========================================

# Database Host (für externe DB)
# DB_HOST=db
# DB_PORT=3306

# Redis (für externe Redis)
# REDIS_CACHE=redis-cache:6379
# REDIS_QUEUE=redis-queue:6379

# Nginx Timeouts
PROXY_READ_TIMEOUT=120
CLIENT_MAX_BODY_SIZE=50m
